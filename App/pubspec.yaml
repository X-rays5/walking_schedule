name: walking_schedule
description: none

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.2.0+4

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  intl: ^0.18.1

  http: ^1.1.0

  quiver: ^3.1.0

  cached_network_image: ^3.3.0

  url_launcher: ^6.1.5
  uni_links: 0.5.1

  firebase_core: ^2.22.0
  firebase_auth: ^4.14.0
  firebase_messaging: ^14.7.4

  awesome_notifications_core: ^0.8.1
  awesome_notifications: any

  google_sign_in: ^6.1.6

dev_dependencies:
  flutter_lints: ^3.0.1
  flutter_launcher_icons: ^0.13.1

flutter_launcher_icons:
  image_path: "assets/logos/app/logo.png"
  android: true
  ios: true

flutter:
  uses-material-design: true

  assets:
    - assets/logos/app/logo.png
    - assets/logos/google/g-round.png